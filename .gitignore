# Dependencies (re-installable via npm install)
node_modules/

# Claude / Cowork local state
.claude/

# Raw downloaded art (large — not needed at runtime; sprites live in assets/)
downloadedart/

# Backup / scratch files
*.clean
*.bak
*.tmp
*.log

# OS files
Thumbs.db
.DS_Store
desktop.ini

# Editor
.vscode/
.idea/
*.swp

# Dev scratch (one-off verification / probe scripts)
# Underscore-prefixed files at the repo root and in tests/ are by convention
# throw-away verification harnesses, not part of the canonical test suite.
_*.js
_*.mjs
_*.txt
tests/_*.mjs
tests/_*.js
art.js.broken
ui.js.broken
game.js.broken
launch-test.js
*.broken